[workspace]
name = "weft"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = "3.12.*"
pip = "*"
pytest = ">=8"
pytest-timeout = "*"
pyyaml = "*"

[pypi-dependencies]
weft = { path = ".", editable = true }

[tasks]
test = "pytest -q"
