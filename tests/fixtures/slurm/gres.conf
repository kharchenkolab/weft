# Fake GPUs for discovery tests: CountOnly skips device-file validation,
# so the scheduler tracks gres/gpu without hardware. Jobs asking --gres
# will allocate against these (and obviously find no real devices).
AutoDetect=off
NodeName=weftslurm Name=gpu Type=fake Count=2 Flags=CountOnly
